<Window x:Class="ToleranceTool.Wpf.Import.AddAccessSourceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Access source" Height="320" Width="480"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">

    <Window.Resources>
        <ResourceDictionary Source="/ToleranceTool.Wpf;component/Theme.xaml" />
    </Window.Resources>

    <DockPanel Margin="14" LastChildFill="True">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="84" Margin="4,0" IsDefault="True" Click="Ok_Click" />
            <Button Content="Cancel" Width="84" Margin="4,0" IsCancel="True" />
        </StackPanel>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Name" Margin="0,0,10,0" />
            <TextBox Grid.Row="0" Grid.Column="1" x:Name="NameBox" />

            <Label Grid.Row="1" Grid.Column="0" Content="Database" Margin="0,0,10,0" />
            <DockPanel Grid.Row="1" Grid.Column="1">
                <Button DockPanel.Dock="Right" Content="Browse…" Padding="8,2" Margin="6,2,0,2" Click="Browse_Click" />
                <TextBox x:Name="DatabaseBox" />
            </DockPanel>

            <Label Grid.Row="2" Grid.Column="0" Content="Query (SQL)" Margin="0,4,10,0" VerticalAlignment="Top" />
            <TextBox Grid.Row="2" Grid.Column="1" x:Name="QueryBox" AcceptsReturn="True" TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto" MinHeight="70" />

            <Label Grid.Row="3" Grid.Column="0" Content="Universal ID column" Margin="0,0,10,0" />
            <TextBox Grid.Row="3" Grid.Column="1" x:Name="KeyBox" Text="UniversalId" />

            <CheckBox Grid.Row="4" Grid.Column="1" x:Name="MasterBox" Content="This source is the master" Margin="0,6,0,0" />
        </Grid>
    </DockPanel>
</Window>
